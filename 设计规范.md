════════════════════════════════════════════════════════════
  基金日报设计规范（FD-HIG）v1.5
  Fund Daily Report Human Interface Guidelines
  生效版本：2.0.0 起全部 UI 施工以本文件为唯一权威
  定案来源：2026-08-09 用户定调 + genui 交互小样评审拍板
════════════════════════════════════════════════════════════

【〇、本规范的地位】
  · 类比苹果 HIG：所有界面（首页/弹窗/设置/详情页）必须照着本规范长出来
  · 代码与规范冲突时：改代码；规范要变时：先改本文件并升版本号，再改代码
  · 1.x 旧 UI 不追溯改造，2.0.0 一次性切换

────────────────────────────────────────────────────────────
一、设计原则（五条，按优先级）
────────────────────────────────────────────────────────────
  1. 数据是主角：界面为数字服务，装饰一律让位。涨跌幅是唯一允许"喊"的元素
  2. 颜色只给信号：界面本体用无彩色（灰阶），彩色只用于涨跌/估值/主操作等语义信号
  3. 一个界面一个重点：每个屏只有一个强调色实底元素（首页是「刷新」）
  4. 诚实标注：数据是什么口径就标什么（净值分位不冒充估值；抓不到就显示"—"，不瞎编）
  5. 全部图案自主原创：图标/插画/标识全部手写 SVG，无 emoji 进界面骨架，无图标库，去 AI 模板感

────────────────────────────────────────────────────────────
二、主题系统（三主题，2.0.0 实现切换）
────────────────────────────────────────────────────────────
  架构：同一套语义令牌（token），三套取值。切换主题 = 换令牌表，组件代码零改动。
  存储：主题选择存 settings.json；「跟随系统」按操作系统明暗自动选 B 暗/B 浅。

  【主题 B · 深空暗】= 默认主题
    底色 bg        #10151c
    卡片 card      #161d27
    发丝线 line    #232b36
    主文字 text    #e8edf3
    次级文字 muted #7d8a99
    点睛色 accent  #3d7eff（电光蓝，仅用于主操作/标识/链接/选中态）
    信号红 up      #ff5d5d    信号绿 down #34c77b
    面积填充       红 rgba(255,93,93,.14)  绿 rgba(52,199,123,.12)
    悬停提亮层     #1b2430

  【主题 B · 晨雾浅色】= B 的亮面变体（明暗切换用，不是另起炉灶）
    bg #f5f7fa / card #ffffff / line #e4e9f0
    text #1a2230 / muted #6b7889
    accent #2563eb（白底对比度加深）
    up #e5484d / down #18a058（饱和度加深保白底对比）
    面积填充 红 rgba(229,72,77,.10)  绿 rgba(24,160,88,.10)

  【主题 A · 纸账本】= 设置里可选的个性皮肤（不占默认位）
    暖纸底 bg #f7f1e6 / 纸卡 card #fffbf2 / 线 line #e2d5b8
    墨字 ink #2b2620 / 次级 muted #8a7f6d
    朱砂 up #c23d2e / 松绿 down #2f7d5c
    差异点：数字用衬线体（Georgia/宋体系），走势线手绘感（圆帽折线+终点圆点），
    卡片带纸纹质感边框，标签用「账本条目」语言风格

  【令牌命名规则】
    代码中禁止硬编码色值；一律引用语义令牌：
    bg / card / line / text / muted / accent / up / down / up_fill / down_fill / hover
    新增颜色必须先在本文件登记语义名，再进代码

────────────────────────────────────────────────────────────
三、颜色使用规则
────────────────────────────────────────────────────────────
  · 涨跌语义：涨=红、跌=绿（中国市场惯例，与西方相反，不可改）
  · 估值信号复用涨跌色：过热=红、低估=绿、中性=琥珀 #b45309（B暗可用 #d97706）
  · accent 电光蓝只用于：主操作按钮、应用标识、链接、选中/聚焦态。禁止用于正文
  · 灰阶承担 90% 的界面：边框、分隔、次级文字、禁用态
  · 禁止彩虹：不允许"一个按钮一个颜色"（旧版顶栏病灶，永久禁止复发）

────────────────────────────────────────────────────────────
四、字体与数字
────────────────────────────────────────────────────────────
  · 界面字体：Windows「Microsoft YaHei」/ macOS「PingFang SC」（沿用 FONT 常量机制）
  · A 纸账本皮肤的数字：衬线体 Georgia（macOS）/ 系统衬线（Windows）
  · 所有金融数字必须 tabular-nums（表格对齐数字），防跳动
  · 字号阶梯（B 主题）：
      大信号数字（卡片涨跌幅）  22px Bold
      概览统计值                18px Semibold
      基金名                    13px Medium
      普通数值                  13px Regular
      标签/代码/辅助            10.5px Regular，字距 +1px，标签可全小写灰
  · 金额格式：¥1,234.56（千分位+两位小数）；收益率 ±x.xx%（强制符号）

────────────────────────────────────────────────────────────
五、布局与间距
────────────────────────────────────────────────────────────
  · 间距阶梯：4 / 8 / 12 / 16 / 24 / 32，只允许取这些值
  · 圆角：卡片 8px，按钮 6px，弹窗 12px，小徽章 4px
  · 卡片内边距：14px 16px；卡片间距 10px
  · 分区靠留白与发丝线（1px line 色），禁止用重阴影和彩色块分区
  · 窗口默认尺寸沿用现有（约 1100×720），左右分栏：左栏图表榜单 / 右栏卡片区

────────────────────────────────────────────────────────────
六、组件规范
────────────────────────────────────────────────────────────
  【顶栏】（病灶改造重点，层级铁律）
    左：原创 SVG 标识 + 「基金日报」+ 版本徽章
    次：账户切换器 = 独立下拉控件，不混进按钮群
    导航群：统一细线幽灵按钮（透明底/1px line 边/悬停提亮），同权重无彩虹
    工具群：导出/备份/关于 = 30×30 纯图标小按钮归组最右，悬停显名
    主操作：全顶栏唯一强调色实底按钮「刷新」
  【基金卡片】
    三段式：名称区（名/代码/估值信号行）+ 数值区（净值/持有/今日/累计竖排）+ 涨跌幅主角
    估值信号行格式：「🔴 过热 · PE 88%分位」用文字色点，色点用图形绘制不用 emoji
    走势图：面积折线（涨跌对应填充色），高 28px，右对齐
    按钮：「详情」为卡片内唯一浅色轻按钮；「标记清仓」为灰描边弱按钮
  【按钮家族】（全局只允许四种）
    Primary：accent 实底白字（每屏最多一个）
    Ghost：透明底+1px line 边（导航/次要操作）
    Soft：accent 10% 透明底+accent 字（卡片内轻操作）
    Danger：红色描边红字（删除类，需二次确认弹窗）
  【提醒条】
    顶部下方通栏，card 底+1px line 边；信号文字用对应语义色，分隔用「｜」
  【弹窗】
    12px 圆角，标题 15px Medium，内容区与按钮区用留白分隔；
    确认类弹窗默认焦点在「取消」，防误触（删账户等破坏性操作必须二次确认）
  【设置页】（3.0.0 前为设置弹窗，先按此规范排版）
    分组卡片：组标题 13px Medium + 组内条目（图标+标题+说明小字+右侧控件）
    「外观」组承载主题切换：下拉选项=跟随系统/深空暗/晨雾浅色/纸账本
  【表格/日历】
    行高 36px，斑马纹用 card 与 bg 交替，数字列右对齐 tabular-nums

────────────────────────────────────────────────────────────
七、图标规范（原创体系）
────────────────────────────────────────────────────────────
  · 画布：24×24 网格，有效区 20×20 居中
  · 线宽：1.5~1.6px，圆帽圆角连接（stroke-linecap/linejoin: round）
  · 颜色：默认 muted，激活/点睛用 accent 或 text；禁止彩色图标
  · 风格：几何+单笔走势隐喻（标识=圆角方框内上升折线+定位点），全项目统一笔性
  · 禁止：emoji 进入按钮/导航/标题等界面骨架（A 纸账本皮肤内可少量手绘符号）
  · 图标清单（首批）：标识/刷新/导出/备份/关于/持仓/收益/交易/诊断/账户/主题/涨跌箭头
    全部手写 SVG 源文件随 2.0.0 代码入库（内联字符串形式，无外部图标库）

────────────────────────────────────────────────────────────
八、动效
────────────────────────────────────────────────────────────
  · 时长：微交互 120ms / 面板切换 200ms / 数字刷新 250ms，缓动 ease-out
  · 允许：卡片淡入、数值渐变、弹窗淡入缩放 0.98→1
  · 禁止：弹跳/旋转/闪烁等炫技动效；禁止让动效阻塞数据呈现

────────────────────────────────────────────────────────────
九、可访问性
────────────────────────────────────────────────────────────
  · 正文对比度 ≥ 4.5:1（B 浅主题的 muted 已按此校准，改色必须复测）
  · 涨跌不只靠颜色：涨跌幅自带 ± 符号，估值带文字档位（过热/中性/低估）
  · 触控/点击目标 ≥ 28px；键盘可达：弹窗支持 Esc 关闭

────────────────────────────────────────────────────────────
十、跨平台适配
────────────────────────────────────────────────────────────
  · 字体按 FONT 常量走平台分支；macOS 窗口控件在左、Windows 在右（沿用系统原生标题栏，不自绘）
  · macOS 上卡片圆角视觉略放大 1px 抵消抗锯齿发虚（可选微调）
  · 两端必须呈现完全一致的布局与色彩（同一套 QSS 令牌生成）

────────────────────────────────────────────────────────────
十一、技术落地约定（2.0.0 实现层）
────────────────────────────────────────────────────────────
  · 主题引擎：Python 侧维护令牌字典 → 运行时生成 QSS 字符串 → app.setStyleSheet
  · 切换即时生效，选择持久化到 settings.json（schema 只增不改）
  · 组件样式只写令牌占位符，禁止在组件里散落色值
  · 旧 UI 的 emoji 按钮文案（💼 管理持仓等）在 2.0.0 统一换成「图标+文字」

────────────────────────────────────────────────────────────
十二、版本与变更管理
────────────────────────────────────────────────────────────
  · 本规范版本独立演进：v1.0（2026-08-10，首版定案）
  · 改规范 = 改宪法：须先在本文件修订并记变更日志，再动代码
  · 评审流程沿用已验证模式：genui 出小样 → 用户评审拍板 → 定稿写入本规范 → 施工

【变更日志】
  v1.0 2026-08-10 首版：三主题令牌、顶栏层级铁律、组件/图标/动效规范定案
  v1.1 2026-08-10（v1.7.0 落地补充）：① 新增第四套令牌 classic（1.x 原样，默认主题，保视觉零变化，2.0.0 卸任）；
    ② 令牌表补充实现层辅助色：text_sub/faint/cleared_bg/cleared_border/card_hover/panel_border/
    mid_val/accent_hover/accent_soft/accent_soft_hover/btn_disabled_bg（语义不变：彩色只给信号）；
    ③ 实现细化：采用“Python 令牌字典→组件内联样式”路线（而非纯 QSS 生成），
    与存量内联样式代码库兼容，切换=换令牌表+重刷。
  v1.2 2026-08-10（v2.0.0 落地补充）：① 新增 hover_bg 令牌（幽灵按钮悬停底）；② 默认主题正式切为 b_dark，
    classic 降为可选怀旧项（弹窗类控件 classic 保原生外观，其余主题走 global_qss）；
    ③ 图标首批落地：QPainter 直画（无 QtSvg 依赖），含 logo/refresh/export/backup/about/hold/pnl/trades/diag/settings；
    ④ 顶栏 emoji 清零完成；弹窗标题 emoji 清理列入后续（不在 v2.0.0 范围，不阻塞）。
  v1.3 2026-08-10（v2.0.0 发布前返工）：用户实机捆出“半黑灾难”（弹窗群未焕新），按铁律拒绝全量重写，
    改为样式层系统性返工：弹窗群（持仓/记账/交易/收益/粘贴/导出）全部令牌化，彩虹按钮退役归入四种按钮家族；
    收益日历格子改主题感知混色；新增辅助令牌函数（table_qss/panel_label_qss/danger_btn_qss 等）与 dark 标志。
  v1.4 2026-08-10（用户五轮实测反馈后令牌修正）：b_dark 次级文字/分割线整体提亮（对比度优先：
    card_border #232b36→#313d4d，text_sub→#c9d2dc，muted→#97a4b4，faint→#707e8e）；
    下拉框定形为 Win11 风格（圆角/扁平/hover+focus 态/CSS 三角箭头，禁原生斜边框）；
    新增约束：任何主题化控件必须同时进构建与 _restyle_all 两条路径（防切主题后保持旧色）。
  v1.5 2026-08-10（用户实测捆出箭头渲染问题后修正）：① CSS 边框三角在 Qt 里实测渲染成横条，弃用；
    下拉/日期控件箭头改为 QPainter 直画存 PNG、QSS 按绝对路径引用（_arrow_png，随主题色生成）；
    ② global_qss 改为四主题全应用（classic 不再豁免原生外观），保全程序弹窗/按钮样式统一；
    ③ 经验：Qt 样式表不可靠技巧（边框三角/零尺寸子控件）一律不用，图形资产走 QPainter 直画。
════════════════════════════════════════════════════════════
